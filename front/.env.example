# Base URL of the Go backend API.
# Copy this file to `.env` and adjust if your backend runs elsewhere.
# If unset, the app falls back to http://localhost:8081 (see src/api/client.ts).
VITE_API_URL=http://localhost:8081
